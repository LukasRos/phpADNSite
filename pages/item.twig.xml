<item>
<title>{% if post.repost %}Repost from {{post.user.name}}: {% endif %}{{post.text|slice(0,80)}}{% if post.text|length > 80 %} ...{% endif %}</title>
<description>{% if post.repost %}reposted from <img src="{{post.user.avatar_image.url}}" /> <a href="{{post.user.url}}">{{post.user.name}} (@{{post.user.username}})</a>: {% endif %}{{post.html}}</description>
<pubDate>{{post.created_at|date('r')}}</pubDate>
<guid>{{site_url}}post/{{post.id}}"></guid>
<link>{{site_url}}post/{{post.id}}"></link>
{% for tag in post.tags %}<category>{{tag}}</category>{% endfor %}
</item>	